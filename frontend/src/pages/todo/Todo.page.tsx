import React from "react";
import {FormLayout} from "../../shared/layouts/FormLayout";

const TodoPage = () =>{
    return(
        <FormLayout>
            {/*<h1>{`${name} todos`}</h1>*/}
        </FormLayout>
    )
};

export default TodoPage;