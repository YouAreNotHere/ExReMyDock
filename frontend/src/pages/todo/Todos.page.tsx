import React from "react";
import {FormLayout} from "../../shared/layouts/FormLayout";

const TodosPage = () =>{
    return(
        <FormLayout>
            Todos list
            {/*<h1>{`${name} todos`}</h1>*/}
        </FormLayout>
    )
};

export default TodosPage;